// Creating an object of type "person"
new Person {
      // Providing values to the object attributes
      name = "Majdi"
      job  = "Doctor"
      age  = 42
      // Calling the info() method in the object
      info()
}
// Class description
class Person
{
           name
           job
           age
           def info()
           {
                ? "Name : " + name
                ? "Job : " + job
                ? "Age : " + age
           }
}
